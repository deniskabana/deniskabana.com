import { css } from "@emotion/css"

export const styles = {
  headerButton: css`
    svg {
      margin-right: 0.5rem;
    }
  `,
  wrapper: css`
    display: flex;
    justify-content: flex-end;
    padding: 1rem 0;
    gap: 0.75rem;
  `,
}
